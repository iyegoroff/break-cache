define(function (require) {
    const bdd = require('intern!bdd');
    const expect = require('intern/chai!expect');
    const cacheBreaker = require('intern/dojo/node!../../break-cache.js');

    bdd.describe('API', () => {
        bdd.it('should pass input file contents to stdout unmodified', () => {
            expect()
        })
    });
});
